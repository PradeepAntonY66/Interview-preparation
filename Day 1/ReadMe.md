# Todo

# Week 1: Arrays DSA.
1. Linear Search
2. Largest Element
3. Second Largest Element
4. Check if Array is Sorted


# Things learned while solved array problems
1. Check is array is null or empty.
2. NullPointerException will be thrown if we don't properly handle array = null
3. If array is empty, then arr.length == 0 [empty array].
        
4.      if (arr == null || arr.length == 0) {
        throw new IllegalArgumentException("Array must not be null or empty.");
        }